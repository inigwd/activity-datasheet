class ActiveDay {
    constructor(date, project, start, end, pause, comment) {
        this.date = date
        this.project = project
        this.start = start
        this.end = end
        this.pause = pause
        this.comment = comment
    }
}
export default ActiveDay